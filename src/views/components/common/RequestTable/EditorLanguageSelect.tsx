import { Select } from "antd";
import React from "react";

enum Language {
  PLAIN_TEXT = "plaintext",
  JSON = "json",
}

export default function EditorLanguageSelect() {
  return <Select>Select.Option</Select>;
}
